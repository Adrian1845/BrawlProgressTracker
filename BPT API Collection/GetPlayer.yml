info:
  name: GetPlayer
  type: http
  seq: 1

http:
  method: GET
  url: http://127.0.0.1:8000/api/player/{{hashtag}}{{playerTag}}
  auth: inherit

runtime:
  variables:
    - name: playerTag
      value: 2PPVPRGP
    - name: hashtag
      value: "%23"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
